[project]
name = "comfyui-mbqwedge"
version = "1.2.0"
description = "Compare ComfyUI parameter values without contact sheets — sweep numerics, samplers, or schedulers, then inspect each result full-res in MBQ Viewer."
license = { file = "LICENSE" }
readme = "README.md"

[project.urls]
Repository = "https://github.com/Beakfx/mbq"

[tool.comfy]
PublisherId = "beakfx"
DisplayName = "MBQ Wedge"
Icon = "https://raw.githubusercontent.com/Beakfx/mbq/main/assets/mbq_icon.png"
Tags = ["parameter wedge", "sweep", "workflow", "utility"]
